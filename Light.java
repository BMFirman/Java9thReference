// Compute the distacne light travels
class Light {
	public static void main(String[] args) {
		int lightspeed;
		long days
		long seconds
		long distance

		// approximate speed of light in in km/s
		lightspeed = 300000000;

		days = 1000; // specify the number of days

		seconds = days * 24 * 60 * 60 // conversion to seconds

		distacne = seconds * lightspeed; // compute the distance

		System.out.print("In " + days + "Light will have traveled " + distance);
	}
}